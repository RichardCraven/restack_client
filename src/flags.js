/**
 * flags.js
 * Feature flags for toggling experimental / redesigned UI features.
 *
 * mapRedux  – true  → use the redesigned MapRedux map screen (vertical
 *                      timeline + zone network panel).
 *             false → keep the original isometric-slab dungeon map view.
 */
export const FLAGS = {
    mapRedux: true,
};

export default FLAGS;
